launchctl setenv OLLAMA_HOST "0.0.0.0:11434"
Run stack:

1) Place dev TLS certs in certs/ as localhost.crt and localhost.key
2) Run ./start.sh to build and start containers, pull Ollama model, and sideload the Word add-in.
3) Open Word and accept the sideload prompt if shown.