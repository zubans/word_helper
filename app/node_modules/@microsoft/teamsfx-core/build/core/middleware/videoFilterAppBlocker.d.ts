import { NextFunction } from "@feathersjs/hooks";
import { FxError, Result } from "@microsoft/teamsfx-api";
import { CoreHookContext } from "../types";
export declare function isVideoFilterProject(projectPath: string): Promise<Result<boolean, FxError>>;
/**
 * This middleware will block remote operations (provision/deploy/...) since we don't support these operations for now.
 */
export declare const VideoFilterAppBlockerMW: (ctx: CoreHookContext, next: NextFunction) => Promise<void>;
//# sourceMappingURL=videoFilterAppBlocker.d.ts.map